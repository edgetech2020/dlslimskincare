import 'package:flutter/material.dart';

class Testist extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Image(
        image: AssetImage('assets/images/gmail_logo.png'),
      ),
    );
  }
}
